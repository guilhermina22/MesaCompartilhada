document.addEventListener('DOMContentLoaded',()=>{const bar=document.querySelector('.mc-navbar');if(!bar)return;const toggle=bar.querySelector('.mc-menu-toggle');const account=bar.querySelector('.mc-account');const accountBtn=bar.querySelector('.mc-account__button');if(toggle)toggle.addEventListener('click',()=>{const open=bar.classList.toggle('is-menu-open');toggle.setAttribute('aria-expanded',String(open))});if(accountBtn)accountBtn.addEventListener('click',e=>{e.stopPropagation();const open=account.classList.toggle('is-open');accountBtn.setAttribute('aria-expanded',String(open))});document.addEventListener('click',e=>{if(account&&!account.contains(e.target)){account.classList.remove('is-open');if(accountBtn)accountBtn.setAttribute('aria-expanded','false')}});window.addEventListener('resize',()=>{if(window.innerWidth>720){bar.classList.remove('is-menu-open');if(toggle)toggle.setAttribute('aria-expanded','false')}})});
